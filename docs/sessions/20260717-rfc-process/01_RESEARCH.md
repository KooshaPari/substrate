# Research

- `audit_scorecard.json` marks DOC-17 missing because `docs/RFC.md` did not
  exist.
- `CONTRIBUTING.md` sent change proposers to an `rfc` label with "template
  TBD".
- The existing feature-request template already asks for motivation, proposal,
  alternatives, scope, compatibility, rollout, and testing.
- Existing decision records under `docs/adr/` establish the durable-decision
  format this RFC process references.

The RFC process therefore reuses those contracts rather than creating a
parallel governance system.
