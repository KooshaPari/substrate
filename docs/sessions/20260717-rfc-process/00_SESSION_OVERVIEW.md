# RFC process documentation

## Goal

Close DOC-17 with a canonical, repository-native RFC process that makes the
existing `rfc` contribution path actionable.

## Scope

The change adds the RFC lifecycle and template, links it from the contributing
guide, and provides a focused verifier. It does not change crate APIs,
package/release policy, or the separate gateway watcher repair.
