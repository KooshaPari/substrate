# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.82"
name = "phenotype-router"
version = "0.2.0"
build = false
publish = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Phenotype-owned router decision layer (ADR-050 / ADR-051, §8 router-architecture ACCEPTED 2026-06-20). Hexagonal L4 Port/Adapter primitive (ADR-038) with OTel-native OTLP span emission (ADR-012 / ADR-036B)."
readme = "README.md"
keywords = [
    "phenotype",
    "router",
    "llm",
    "routing",
    "otel",
    "substrate",
    "port-adapter",
]
categories = [
    "api-bindings",
    "asynchronous",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/KooshaPari/phenotype-router"

[features]
bifrost = []
chaos = []
default = ["otlp"]
otlp = []

[lib]
name = "phenotype_router"
path = "src/lib.rs"

[[test]]
name = "chaos_matrix"
path = "tests/chaos_matrix.rs"

[[test]]
name = "decision_e2e"
path = "tests/decision_e2e.rs"

[[test]]
name = "hello_world"
path = "tests/hello_world.rs"

[[test]]
name = "otlp_smoke"
path = "tests/otlp_smoke.rs"

[[test]]
name = "plugin_chain"
path = "tests/plugin_chain.rs"

[[bench]]
name = "decision"
path = "benches/decision.rs"
harness = false

[dependencies]
async-trait = "0.1"
thiserror = "1"
tracing = "0.1"

[dependencies.serde]
version = "1"
features = ["derive"]

[dependencies.tracing-subscriber]
version = "0.3"
features = [
    "env-filter",
    "fmt",
]

[dev-dependencies]
rand = "0.8"
serde_json = "1"

[dev-dependencies.criterion]
version = "0.5"
features = [
    "cargo_bench_support",
    "rayon",
    "plotters",
]
default-features = false

[dev-dependencies.tokio]
version = "1"
features = [
    "macros",
    "rt-multi-thread",
    "time",
    "test-util",
]

[workspace]
