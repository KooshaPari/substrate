### L96 — Design Token System
score: 0  glyph: ✗
evidence:
  - `rg -n "theme|font-family|font-face|token|palette|Color::|#[0-9a-fA-F]{6}" .` only surfaces incidental mentions in docs/scripts, not a committed token source
  - `rg -n "src/theme|styles/tokens|palette.rs|tailwind.config" .` found no token file or theme module
  - `rg -n "#[0-9a-fA-F]{6}" crates/ src/` found no committed token table to act as a single source of truth
gaps:
  - Add a committed token file for color/type/spacing/radius/motion — effort: M
soft_goal_delta: no token system is committed yet

### L97 — Typography System
score: 0  glyph: ✗
evidence:
  - `rg -n "@font-face|font-family|next/font|fontsource|mono|measure|max-width|line-height" .` found no loaded/declared type system in source
  - `rg -n "Style::|Modifier::|border|Rounded" crates/` only shows generic styling APIs, not a documented typography stack
  - `rg -n "font|type scale|typography" docs/ src/` found no repo-level typographic rationale
gaps:
  - Define and document a named type scale plus body measure/line-height — effort: M
soft_goal_delta: typography is implicit, not designed

### L98 — Icon Set
score: 1  glyph: △
evidence:
  - `rg -n "icon|icons|AppIcon|tray|menubar|favicon|icns|ico|iconset" .` finds icon-related references only in docs and build comments
  - `rg -n "lucide|heroicons|tabler|feather|phosphor" .` did not show a committed icon-system dependency split
  - `rg -n "\.icns|\.ico|iconset" .` shows no committed icon asset ladder in the repo
  - `docs/PILOT_RESULTS.md:88` notes substrate returns structured JSON while native codex emits banners/stderr, but that is not a visual asset
gaps:
  - Commit a real icon asset set and provenance note — effort: M
soft_goal_delta: iconography is referenced, not shipped as a set

### L99 — Loading & Skeleton States
score: 0  glyph: ✗
evidence:
  - `rg -n "skeleton|shimmer|placeholder|loading|ProgressBar|Spinner|Gauge|indicatif" .` only surfaced generic mentions, no designed loading surface
  - `rg -n "isLoading|Suspense|loading" crates/ src/` did not expose a thematic async loading system
  - `rg -n "spinner|progress|status line" docs/` found no visual loading spec
  - `scripts/audit.sh:20` references `--no-banner`, which is build tooling not a user-facing loading treatment
gaps:
  - Add designed loading/progress surfaces for async flows — effort: M
soft_goal_delta: no thematic loading state is present

### L100 — Empty & Zero-Data States
score: 0  glyph: ✗
evidence:
  - `rg -n "empty|NoData|zero state|no results|get started|try:" .` did not find designed empty-state copy or components
  - `rg -n "empty state|zero-data|empty-state" docs/ src/` found no explicit empty-state spec
  - `rg -n "nothing to|no results|get started" crates/ src/` found no actionable CLI empty-output pattern
  - `docs/` contains pilot/results material, but no empty-state design reference

gaps:
  - Add explicit empty-state copy/actions for zero-data views — effort: M
soft_goal_delta: empty-state guidance is missing

### L101 — Error & Failure State Design
score: 1  glyph: △
evidence:
  - `rg -n "ErrorBoundary|error.*state|<Error|panic|traceback|eprintln!\(|error formatter" crates/ src/` shows structured error handling in code, but not a designed user-facing error surface
  - `docs/PILOT_RESULTS.md:88` contrasts structured JSON envelopes with raw stderr banners, implying some output shaping exists
  - `rg -n "retry|Go back|help|docs_url|recovery" .` did not show explicit recovery copy or error-view assets

gaps:
  - Add designed error presentation with recovery actions — effort: M
soft_goal_delta: error handling is structured but not visually designed

### L102 — Motion & Animation System
score: 0  glyph: ✗
evidence:
  - `rg -n "animation|motion|transition|spring|lottie|shader|ease" .` found no committed motion system or animation assets
  - `rg -n "Animated|animate|framer-motion|bevy|ratatui" crates/ src/` did not surface a deliberate motion layer
  - `flamegraph.svg:2` contains only SVG text styling, not product motion

gaps:
  - Add a named motion/transition system or animated treatment — effort: L
soft_goal_delta: motion is not a product element yet

### L103 — Splash / Launch Experience
score: 0  glyph: ✗
evidence:
  - `rg -n "splash|launch|startup|banner|ascii art|logo" crates/ src/ docs/` found no splash screen or launch art asset
  - `rg -n "banner|ASCII|spinner|branding" crates/ src/` only showed tool-output comments and audit flags
  - `scripts/audit.sh:20` uses `--no-banner`, which is an ops concern, not a launch experience

gaps:
  - Add a branded startup/splash treatment for the user-visible surface — effort: M
soft_goal_delta: startup branding is absent

### L104 — Theming (Dark / Light / Brand)
score: 0  glyph: ✗
evidence:
  - `rg -n "dark|light|theme|color-scheme|prefers-color-scheme|ThemeProvider|useTheme|data-theme" .` did not expose a runtime theme switch or persisted theme state
  - `rg -n "src/theme|palette|tokens" .` found no theme module to derive alternate modes from
  - `rg -n "--theme" crates/ src/` found no CLI theme flag or palette selector
gaps:
  - Implement dark/light/brand theme modes with persistence or flag control — effort: M
soft_goal_delta: no theme system is committed

### L105 — Visual Consistency & Brand Cohesion
score: 1  glyph: △
evidence:
  - `rg -n "button|card|input|modal|table|spinner|banner" crates/ src/` indicates repeated UI concepts, but no shared visual system is evident from grep alone
  - `rg -n "docs/visual|design system|brand guide|visual reference" .` found no committed visual-reference folder
  - `rg -n "#[0-9a-fA-F]{6}|[0-9]+px" crates/ src/` did not expose a cohesive documented spacing/color scale in source

gaps:
  - Add a docs/visual reference and shared UI primitives — effort: M
soft_goal_delta: visual cohesion is implicit only

### L106 — Signature / Unique Identity Element
score: 0  glyph: ✗
evidence:
  - `rg -n "mascot|signature|identity|unique|themed|personality|brand mark|logo" .` found no named identity element in docs or code
  - `rg -n "animated|interactive|motif|distinctive layout|color story" .` found no product-specific visual signature
  - `rg -n "docs/visual/IDENTITY.md|IDENTITY.md" .` found no identity doc

gaps:
  - Define and implement one memorable signature element — effort: L
soft_goal_delta: product reads as generic from repo evidence

### L107 — Visual Spec & Golden Tests
score: 0  glyph: ✗
evidence:
  - `rg -n "VISUAL_SPEC|docs/visual|PROVENANCE|golden|snapshot|chromatic|playwright|screenshot" .` found no visual-spec folder or golden-output test corpus
  - `rg -n "tests/golden|tests/visual|.snap|.png|.svg" .` showed no committed visual regression fixtures tied to a spec
  - `rg -n "AGENTS.md|visual spec|provenance" docs/` found no agent-facing visual contract

gaps:
  - Add docs/visual plus golden or screenshot regression tests — effort: L
soft_goal_delta: no visual-spec or regression guard exists

CLUSTER_TOTAL score=2/36 pct=6% grade=F
CLUSTER_DONE cluster=C10 repo=substrate
