CLUSTER_START cluster=C07 repo=substrate pillars=L61-L70 date=2026-07-05

### L61 — Devcontainer / .devcontainer
score: 0  glyph: ✗
evidence:
  - rg found no `.devcontainer/` directory or devcontainer config in the repository root scan (rg --files)
  - repository files show Docker- and compose-adjacent docs, but no one-command devcontainer entrypoint (compose/README.md:1)
gaps:
  - Add `.devcontainer/` with post-create setup and pinned features — effort: M
soft_goal_delta: no containerized one-click setup path yet

### L62 — Task runner
score: 0  glyph: ✗
evidence:
  - rg found no `Taskfile.yml`, `mise.toml`, or `justfile` in the repository root scan (rg --files)
  - README documents build and workflow notes, but not a unified task surface (README.md:1)
gaps:
  - Add a task runner exposing build/test/lint/format/bench targets — effort: M
soft_goal_delta: command surface is still README-driven

### L63 — EditorConfig + format-on-save
score: 0  glyph: ✗
evidence:
  - rg found no `.editorconfig` or `.rustfmt.toml` in the repository root scan (rg --files)
  - CI checks `cargo fmt --all -- --check`, but that is only a gate and not an editor contract (.github/workflows/ci.yml:23)
gaps:
  - Add `.editorconfig` plus formatter config for Rust and any other languages — effort: S
soft_goal_delta: formatting is enforced in CI, not standardized locally

### L64 — Test pyramid
score: 2  glyph: ~
evidence:
  - workspace CI runs `cargo test --workspace`, giving a broad unit/integration safety net (.github/workflows/ci.yml:30)
  - repo includes focused integration-style tests across multiple crates, including `engine-forge/tests/real_forge.rs` and `driver-http/tests/api.rs` (crates/engine-forge/tests/real_forge.rs:1; crates/driver-http/tests/api.rs:1)
gaps:
  - Add explicit e2e and chaos layers, plus a documented pyramid map — effort: M
soft_goal_delta: unit+integration coverage is strong, but e2e/chaos are not explicit

### L65 — Mutation testing
score: 0  glyph: ✗
evidence:
  - rg found no `cargo-mutants`, `mutpy`, or `infection` references in the repository scan (rg --files; rg -n)
  - CI workflow only runs fmt/test/clippy, with no mutation stage (.github/workflows/ci.yml:18)
gaps:
  - Add mutation testing to CI, starting with the highest-risk crates — effort: L
soft_goal_delta: no mutation-testing practice yet

### L66 — Property-based testing
score: 1  glyph: △
evidence:
  - repo has a dedicated `crate/` test surface for `store-sqlite`, `substrate-core`, and `a2a`, but no obvious property-testing harness surfaced in the scan (rg --files)
  - existing tests emphasize example-based scenarios and state-machine flows rather than generator-based invariants (crates/substrate-core/tests/fsm.rs:1; crates/store-sqlite/tests/event_store_tests.rs:1)
gaps:
  - Add proptest/quickcheck coverage for claim ordering, DAG invariants, and parser edge cases — effort: M
soft_goal_delta: example tests exist; invariant generators are still missing

### L67 — Fuzz harness
score: 0  glyph: ✗
evidence:
  - rg found no `cargo-fuzz` directory or fuzz target in the repository scan (rg --files)
  - no fuzz workflow or corpus management surfaced alongside the current CI jobs (.github/workflows/ci.yml:18)
gaps:
  - Add at least one fuzz harness for the highest-risk parser or protocol boundary — effort: M
soft_goal_delta: no fuzzing entrypoint or corpus exists

### L68 — Flake detection
score: 0  glyph: ✗
evidence:
  - CI workflow has no rerun/quarantine/stats stage beyond the standard test job (.github/workflows/ci.yml:23)
  - repository scan found no flake tracker, quarantine list, or retry telemetry files (rg --files)
gaps:
  - Add rerun-on-fail telemetry and a quarantine workflow for nondeterministic tests — effort: M
soft_goal_delta: flaky-test handling is manual only

### L69 — Cross-platform CI
score: 2  glyph: ~
evidence:
  - release workflow already builds on `ubuntu-latest` and `windows-latest`, showing non-Linux release coverage (.github/workflows/release-binary.yml:16)
  - codebase carries platform conditionals for Windows/macOS in core crates, indicating active cross-platform awareness (crates/engine-forge/src/lib.rs:289; crates/driver-cli/src/main.rs:202)
gaps:
  - Expand CI to run the test matrix across macOS and Windows, not just release builds — effort: M
soft_goal_delta: release artifacts are cross-platform, but test CI remains Linux-centric

### L70 — Reproducible local dev
score: 1  glyph: △
evidence:
  - README includes build/run guidance and architecture notes, but no single `task dev`-style bootstrap command (README.md:1)
  - repo has a workspace layout and explicit crate boundaries that support a repeatable setup path, but no documented one-command seed/verify flow (Cargo.toml:1; README.md:13)
gaps:
  - Add a one-command bootstrap that installs, builds, seeds, and verifies the workspace — effort: M
soft_goal_delta: setup is documented, but not condensed to a single reproducible command

CLUSTER_TOTAL score=6/30 pct=20% grade=D + CLUSTER_DONE cluster=C07 repo=substrate
