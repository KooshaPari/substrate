CLUSTER_START cluster=C06 repo=substrate pillars=L51-L60 date=2026-07-05

### L51 — Pinned dependencies
score: 2  glyph: ~
evidence:
  - Cargo.lock: present at repo root (grep hit; committed lockfile exists)
  - Cargo.toml: workspace deps are centralized, reducing ad hoc version drift
  - crates/a2a/Cargo.toml:13 references workspace-managed license/deps, showing shared pinning discipline
gaps:
  - No explicit dependency verification policy/lockfile audit step in-repo — effort: M
soft_goal_delta: lockfile present; verify-on-CI not evidenced

### L52 — Reproducible builds
score: 1  glyph: △
evidence:
  - README.md: build and test commands are documented for local runs
  - SPEC.md:11 mentions a test matrix but not reproducibility checks
  - compose/README.md:18 documents restart policy, but not bit-identical build verification
gaps:
  - No `SOURCE_DATE_EPOCH` / reproducibility harness / rebuild comparison evidence — effort: M
soft_goal_delta: documented builds; no reproducibility proof

### L53 — Build provenance
score: 0  glyph: ✗
evidence:
  - rg for `slsa`, `provenance`, `attest`, `cosign`, `rekor`, `fulcio` returned no repo evidence
  - .github/ exists, but no provenance workflow surfaced in grep output
gaps:
  - No SLSA or attestation pipeline visible in-repo — effort: L
soft_goal_delta: not started

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - rg for `hermetic`, `offline`, `no network during build` returned no repo evidence
  - build files surfaced by grep do not document network isolation during build
gaps:
  - No hermetic-build enforcement or network-free build guardrails — effort: L
soft_goal_delta: not started

### L55 — Dependency confusion guard
score: 1  glyph: △
evidence:
  - Cargo workspace dependency management is centralized in Cargo.toml / Cargo.lock
  - docs/adr/0001-gateway-polyglot.md:50 references arch enforcement and workspace dependency direction
  - crates/a2a/Cargo.toml:13 comments on adapter/core boundaries, implying scoped internal dependency discipline
gaps:
  - No registry allow-list, mirror, or typosquat defense config found — effort: M
soft_goal_delta: internal scoping exists; registry guard absent

### L56 — Container provenance
score: 1  glyph: △
evidence:
  - compose/README.md:18 documents restart policy for compose services
  - compose/forge-daemon.json:5 defines a health check for the daemon container
  - .github/ exists, so CI scaffolding is present for a signing pipeline

gaps:
  - No image signing or deploy-time verification evidence in grep output — effort: L
soft_goal_delta: container surfaces exist; attestation missing

### L57 — MCP server provenance
score: 1  glyph: △
evidence:
  - crates/phenotype-mcp/Cargo.toml:5 sets license metadata for the MCP crate
  - crates/driver-mcp/Cargo.toml:5 uses workspace license management
  - driver-mcp/dispatch_mcp/health.py:25 includes a dependency check in readiness status

gaps:
  - No pinned/attested server provenance manifest or versioned registry policy found — effort: M
soft_goal_delta: MCP servers are tracked; provenance is not

### L58 — License scan
score: 1  glyph: △
evidence:
  - crates/phenotype-mcp/Cargo.toml:5 and crates/driver-mcp/Cargo.toml:5 both declare license metadata
  - rg for `deny.toml`, `cargo-deny`, and license scan tooling returned no repo evidence
  - README.md and SPEC.md show active repo documentation, but not an allow-list workflow
gaps:
  - No enforced license scanning or copyleft deny policy found — effort: M
soft_goal_delta: license metadata present; scan policy absent

### L59 — Source code provenance
score: 0  glyph: ✗
evidence:
  - rg for `git signing`, `commit signing`, `signed commits`, `branch protection` returned no repo evidence
  - .github/ exists, but no policy surfaced in grep output
gaps:
  - No commit-signing or protected-branch provenance rule visible — effort: M
soft_goal_delta: not started

### L60 — Toolchain pinning
score: 1  glyph: △
evidence:
  - Cargo.toml at repo root establishes a Rust workspace
  - rg for `rust-toolchain.toml`, `.python-version`, `mise.toml`, `.tool-versions` returned no repo evidence
  - README.md and SPEC.md provide usage docs, but no exact toolchain lock
gaps:
  - No pinned toolchain file or CI pin check found — effort: S
soft_goal_delta: toolchain context exists; exact pin missing

CLUSTER_TOTAL score=7/30 pct=23% grade=F
CLUSTER_DONE cluster=C06 repo=substrate
