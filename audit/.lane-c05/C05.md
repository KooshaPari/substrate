CLUSTER_START cluster=C05 repo=substrate pillars=L41-L50 date=2026-07-05

### L41 — Structured logs
score: 2  glyph: ~
evidence:
  - Cargo.toml:98 enables `tracing` as a workspace dependency
  - crates/forge_daemon/src/lib.rs:28 imports `tracing::{debug, info, warn}`
  - crates/dispatch-bridge/src/lib.rs:56 imports `tracing::{debug, error, info, warn}`
gaps:
  - No repo-wide JSON logging policy or dev logfmt formatting contract — effort: M
soft_goal_delta: tracing is present in core Rust services, but uniform structured-output policy is not documented or enforced

### L42 — OpenTelemetry instrumentation
score: 0  glyph: ✗
evidence:
  - rg for `opentelemetry`, `tracing-opentelemetry`, and `otel` only found docs references in SPEC.md, not wired SDK code
  - Cargo.toml:98 shows `tracing`, but no OTel exporter crate is declared
  - driver-mcp/README.md:49 exposes health/readiness/metrics, not trace instrumentation
gaps:
  - No OTel SDK/exporter wiring in any binary — effort: L
soft_goal_delta: observability is still tracing-only; no cross-binary span pipeline exists

### L43 — Metrics emission
score: 1  glyph: △
evidence:
  - driver-mcp/dispatch_mcp/health.py:67 defines Prometheus text metrics output
  - driver-mcp/tests/test_dispatch_server.py:225 verifies metrics payload formatting
  - crate-level `metrics` search returned no broader Rust emission layer
gaps:
  - No repo-wide counters/gauges or RED/USE metrics backbone — effort: L
soft_goal_delta: one Python surface emits Prometheus text, but the rest of the repo is not instrumented

### L44 — Distributed trace propagation
score: 0  glyph: ✗
evidence:
  - rg for `traceparent` returned no code hits in the substrate tree
  - crates/driver-http/src/lib.rs:116 and crates/gateway/src/lib.rs:124 expose HTTP health routes, but no trace-context middleware
  - crates/substrate-tui/src/dispatch_client.rs:39-41 calls `/healthz` without trace headers
gaps:
  - No W3C trace-context propagation across HTTP or dispatch boundaries — effort: L
soft_goal_delta: cross-process requests still do not carry trace context

### L45 — Continuous profiling
score: 0  glyph: ✗
evidence:
  - rg for `pyroscope`, `pprof`, and profiling agents returned only generic build/profile references in docs
  - docs/adr/0001-gateway-polyglot.md:61 mentions profiling as a decision input, not a shipped profiler
  - docs/PILOT_RESULTS.md:13 records a release build, not continuous profiling data
gaps:
  - No continuous profiler or recorded flamegraph pipeline — effort: L
soft_goal_delta: profiling is acknowledged as useful but not instrumented

### L46 — Error budgets + SLOs
score: 0  glyph: ✗
evidence:
  - rg for `SLO`, `error budget`, and `burn rate` found only the dispatch migration plan mention in DISPATCH_MIGRATION_PLAN_2026_06_30.md:189,424
  - No service-level SLO docs or policy files were found in the repo search
  - README.md and crate READMEs document health checks, not availability targets
gaps:
  - No per-service SLO/error-budget policy or burn-rate alert definitions — effort: M
soft_goal_delta: availability is observable, but not governed by explicit SLOs

### L47 — Health endpoints
score: 3  glyph: ✓
evidence:
  - README.md:121 documents `/healthz` on the substrate HTTP server
  - README.md:179 documents `GET /healthz` returning `{ "status": "ok" }`
  - crates/driver-http/src/lib.rs:116 and crates/gateway/src/lib.rs:124 register `/healthz`
  - crates/driver-http/tests/api.rs:106 and crates/gateway/tests/api.rs:52 test the endpoint path
gaps:
  - No `/readyz` or per-dependency readiness gate surfaced from the main HTTP surfaces — effort: M
soft_goal_delta: liveness is well-covered; readiness is only partial

### L48 — Alerting routing
score: 0  glyph: ✗
evidence:
  - rg for `PagerDuty`, `OpsGenie`, and alert-routing keywords returned no repo alerting config
  - No runbook or severity-routing files were found in the substrate tree
  - Existing docs focus on dispatch/health, not incident routing
gaps:
  - No alert-routing policy, severity map, or runbook set — effort: M
soft_goal_delta: alerts are not wired to a paging or chat escalation path

### L49 — Dashboard coverage
score: 1  glyph: △
evidence:
  - crates/substrate-tui/src/components/dashboard.rs defines a dashboard UI and mentions a key-metrics footer
  - crates/substrate-tui/src/dispatch_client.rs:39-41 exposes a health check surface for the UI
  - driver-mcp/README.md:49 documents metrics but not a Grafana/Datadog dashboard asset
gaps:
  - No shared Grafana/Datadog dashboard definitions or lane/org coverage artifacts — effort: M
soft_goal_delta: the product has a TUI dashboard, but not an observability dashboard stack

### L50 — Chaos/load testing
score: 1  glyph: △
evidence:
  - DISPATCH_MIGRATION_PLAN_2026_06_30.md:189 calls out multi-tier load testing as a future priority
  - crates/wave/src/runner.rs:211 and 308 include concurrency-slot control logic, suggesting load-sensitive code paths
  - rg for `game day` and dedicated chaos tooling returned no explicit harnesses
gaps:
  - No committed steady-state load suite, chaos harness, or game-day automation — effort: L
soft_goal_delta: load considerations exist in planning, but resilience testing is not institutionalized

CLUSTER_TOTAL score=7/30 pct=23% grade=F
CLUSTER_DONE cluster=C05 repo=substrate
