### L81.1 — WCAG 2.2 Level A Baseline
score: 1 glyph: △
evidence:
  - No repo-level `axe`/`a11y` scan target found in `Cargo.toml`, `README.md`, or `docs/` (grep over repo root)
  - `docs/PILOT_RESULTS.md:14` records CLI help working, but not accessibility scans
  - `crates/substrate-tui/src/main.rs:1` identifies a user-facing TUI surface that would need baseline AX coverage
gaps:
  - No automated WCAG/Axe/WAVE gate or dedicated accessibility test suite — effort: M
soft_goal_delta: TUI exists; baseline a11y automation not yet wired

### L81.2 — WCAG 2.2 Level AA Color Contrast
score: 1 glyph: △
evidence:
  - `crates/substrate-tui/src/proccompose.rs:36` uses ratatui colour styling for status badges
  - `crates/substrate-tui/src/statusbar.rs:15` renders a visible status bar, but contrast policy is undocumented
  - No design token or contrast-ratio documentation found in `README.md`, `SPEC.md`, or `docs/` (grep over repo root)
gaps:
  - No documented contrast ratios or automated contrast enforcement — effort: M
soft_goal_delta: status styling exists; contrast policy not encoded

### L82.1 — Keyboard Navigation & Focus Management
score: 2 glyph: ~
evidence:
  - `crates/substrate-tui/src/statusbar.rs:28` advertises `[Tab] switch tab` and `[h] help` keyboard bindings
  - `crates/substrate-tui/src/components/mod.rs:1` defines tab-indexed TUI widget traits
  - `crates/substrate-tui/src/main.rs:17` lists modular TUI screens (`app, config, dispatch_client, components, proccompose, statusbar, help`)
gaps:
  - No explicit focus-trap / focus-return tests or keyboard-only interaction spec found — effort: M
soft_goal_delta: keyboard-driven navigation is present; focus rules are implicit

### L82.2 — Screen Reader & Semantic Announcements
score: 0 glyph: ✗
evidence:
  - No `aria-`/`role=`/screen-reader annotations found in the repo grep pass
  - `crates/substrate-tui/src/main.rs:1` and `crates/substrate-tui/src/statusbar.rs:2` show a raw terminal UI, but no semantic accessibility layer
  - No docs mention screen-reader behavior or accessible narration for CLI/TUI output
gaps:
  - No announced semantics / SR support / accessible narration for TUI or CLI — effort: M
soft_goal_delta: interaction is keyboardable, not screen-reader-aware

### L82.3 — Form/Input Error Recovery
score: 2 glyph: ~
evidence:
  - `crates/driver-cli/src/main.rs:35` sets `arg_required_else_help = true`, reducing empty/incomplete CLI submissions
  - `crates/driver-cli/src/main.rs:40` includes `after_help` examples for common task entrypoints
  - `crates/driver-cli/src/cloud_dispatch.rs:61` and `:62` show explicit status polling and state matching for remote jobs
gaps:
  - No visible error-recovery UX spec for malformed prompts or partial TUI state — effort: S
soft_goal_delta: CLI guides users well; richer recovery copy not documented

### L83.1 — Discoverability & Help Surfaces
score: 3 glyph: ✓
evidence:
  - `crates/driver-cli/src/main.rs:40` includes `after_help` examples for the CLI
  - `README.md:32` documents how to build the dogfooded CLI binary
  - `docs/PILOT_RESULTS.md:14` and `:17` confirm `substrate dispatch --help` works and help renders correctly
gaps:
  - No gap blocking discoverability; only deeper help refinement remains — effort: S
soft_goal_delta: help output and docs are present and validated

### L83.2 — Error Message Clarity
score: 2 glyph: ~
evidence:
  - `crates/driver-cli/src/main.rs:128` wraps prompt-file reads with `with_context(|| format!("read prompt file {}", path.display()))`
  - `crates/driver-cli/src/main.rs:139` resolves prompt text before dispatch, making failures attributable to input parsing vs execution
  - `crates/driver-cli/src/cloud_dispatch.rs:61` uses explicit `poll_status` context around remote dispatch failures
gaps:
  - No dedicated error-message style guide or golden corpus for failed commands — effort: S
soft_goal_delta: contextual errors exist; consistency is not yet enforced

### L84.1 — Documentation Clarity for Humans
score: 3 glyph: ✓
evidence:
  - `README.md:7` explains the three product surfaces (CLI, HTTP, MCP)
  - `README.md:106`-`README.md:121` enumerate adapter responsibilities and exposed routes
  - `SPEC.md:27` points readers to `README.md` for the user-facing API and docs for internals
gaps:
  - No blocking documentation gap found in repo grep pass — effort: S
soft_goal_delta: architecture and entrypoints are well documented

### L84.2 — User Guidance for Common Tasks
score: 2 glyph: ~
evidence:
  - `README.md:32` shows build instructions for the dogfooded binary
  - `README.md:179`-`README.md:202` list route shapes and example `curl` calls
  - `docs/PILOT_RESULTS.md:14` captures a working `dispatch --help` invocation for quick orientation
gaps:
  - No task-oriented quickstart beyond the core dispatch/build path — effort: S
soft_goal_delta: common flows are documented, but not fully task-oriented

### L85.1 — CLI Output Legibility
score: 2 glyph: ~
evidence:
  - `crates/substrate-tui/src/statusbar.rs:15` and `:28` show a compact status bar with keyboard hints
  - `crates/substrate-tui/src/main.rs:17` keeps the TUI split into named modules instead of one monolith
  - `README.md:3`-`README.md:8` presents a concise product summary and surface taxonomy
gaps:
  - No visual-output golden tests or typography/readability spec for terminal layouts — effort: M
soft_goal_delta: output is organized, but readability is not formally tested

### L85.2 — Interaction Feedback & State Visibility
score: 2 glyph: ~
evidence:
  - `crates/substrate-tui/src/statusbar.rs:16` derives a status string from `app.connected`
  - `crates/substrate-tui/src/proccompose.rs:93` and `:116` model composition status and health checks
  - `crates/driver-cli/src/cloud_dispatch.rs:61` polls remote status before finalizing a result
gaps:
  - No documented live-region-like feedback or event transcript for state changes — effort: S
soft_goal_delta: operational state is visible, but not narratively surfaced

### L86.1 — Documentation-as-Context for Agents
score: 3 glyph: ✓
evidence:
  - `llms.txt` exists at repo root and is referenced by the repo grep pass
  - `README.md:3` and `:7` provide a high-signal product overview and surface map
  - `SPEC.md:27` explicitly routes readers to the README for user-facing API guidance
gaps:
  - No blocking agent-context gap found during grep pass — effort: S
soft_goal_delta: agent-facing context is already reasonably strong

### L86.2 — Task-Oriented Onboarding
score: 1 glyph: △
evidence:
  - `README.md:32` documents the build command, and `README.md:60`-`README.md:63` include usage examples
  - `docs/PILOT_RESULTS.md:138` summarizes deployment/phase notes and known gaps
  - `crates/driver-cli/src/main.rs:40` provides help examples directly in the binary
gaps:
  - No single first-run onboarding path or “what to do next” flow for new users — effort: M
soft_goal_delta: building blocks exist; guided onboarding is partial

### L87.1 — Help, Hints, and Progressive Disclosure
score: 3 glyph: ✓
evidence:
  - `crates/driver-cli/src/main.rs:40` ships examples in `after_help`
  - `crates/substrate-tui/src/statusbar.rs:28` exposes inline keyboard hints
  - `docs/PILOT_RESULTS.md:14` confirms the help command renders correctly in practice
gaps:
  - No material gap found in the grep pass — effort: S
soft_goal_delta: hints are present in both CLI and TUI

### L87.2 — Accessible Defaults & Non-Color Fallbacks
score: 1 glyph: △
evidence:
  - `docs/PILOT_RESULTS.md:17` confirms build/help behavior, but not non-color terminal handling
  - `crates/substrate-tui/src/proccompose.rs:36` uses colors for status, implying color-dependent affordances
  - No `NO_COLOR`/`TERM=dumb` handling found in the grep pass
gaps:
  - No documented non-color fallback policy for TUI output — effort: S
soft_goal_delta: colorized terminal output exists; fallback behavior is undocumented

CLUSTER_TOTAL score=20/27 pct=74% grade=C
CLUSTER_DONE cluster=C09 repo=substrate
