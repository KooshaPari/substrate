# thegent-dispatch — AGENTS.md

## Project Overview
Rust CLI tool for agent dispatch and orchestration across the Phenotype org.

## Stack
- Language: Rust (workspace with 6+ crates)
- Build: `cargo build --release`
- Test: `cargo test --workspace`

## Key Commands
- `cargo build --release` — build
- `cargo test --workspace` — test

## Notes
CLI tool for dispatching agents. Follow Phenotype scripting language hierarchy (Rust default). Branch discipline: worktrees for feature work.