[package]
name = "routing-phenotype-router"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "RoutingPort adapter that delegates substrate routing decisions to phenotype-router's decision layer."
publish = false

[dependencies]
substrate-core = { version = "0.3.5", path = "../substrate-core" }
phenotype-router = { git = "https://github.com/KooshaPari/phenotype-router" }
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt"] }
