[package]
name = "substrate-trace"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "TracePort adapters: Noop, Recording, MultiTrace, AgilePlus, Tracera."

[dependencies]
substrate-core = { version = "0.3.2", path = "../substrate-core" }
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "macros", "sync"] }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "macros"] }
