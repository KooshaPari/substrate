[package]
name = "substrate-dag"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "WorkflowPort DAG orchestration via petgraph."

[dependencies]
substrate-core = { version = "0.3.6", path = "../substrate-core" }
petgraph = "0.8"

[dev-dependencies]
