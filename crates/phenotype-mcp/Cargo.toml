[package]
name = "phenotype-mcp"
version = "0.3.9"
edition = "2021"
license = "MIT"
repository = "https://github.com/KooshaPari/substrate"
description = "Phenotype MCP integration adapter for substrate routing and dispatch."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
