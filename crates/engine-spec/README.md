# engine-spec

Provider-agnostic [`TaskSpec`] and [`ArgvBuilder`] trait for mapping neutral task descriptions onto engine CLIs.

```toml
engine-spec = { git = "https://github.com/KooshaPari/substrate", package = "engine-spec" }
```

Included by default in the [`substrate`](../substrate) facade (`spec` feature).
